import React, { useEffect, useState } from "react";
import { InterviewQuestionsCreateCategoryProps } from "./interviewQuestionsCreateCategory.interfaces";
import "./interviewQuestionsCreateCategory.scoped.scss";

export const InterviewQuestionsCreateCategory = ({
  editElementId,
  data,
  setData,
  formWasSubmitted,
  setFormWasSubmitted,
}: InterviewQuestionsCreateCategoryProps) => {
  return <div className="snipets-wrapper">create category inter</div>;
};
