import React, { useEffect, useState } from "react";
import { LanguagesCreateCategoryProps } from "./languagesCreateCategory.interfaces";
import "./languagesCreateCategory.scoped.scss";

export const LanguagesCreateCategory = ({
  editElementId,
  data,
  setData,
  formWasSubmitted,
  setFormWasSubmitted,
}: LanguagesCreateCategoryProps) => {
  return <div className="snipets-wrapper">create category lang</div>;
};
