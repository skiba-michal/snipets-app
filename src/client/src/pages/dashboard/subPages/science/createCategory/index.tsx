import React, { useEffect, useState } from "react";
import { ScienceCreateCategoryProps } from "./scienceCreateCategory.interfaces";
import "./scienceCreateCategory.scoped.scss";

export const ScienceCreateCategory = ({
  editElementId,
  data,
  setData,
  formWasSubmitted,
  setFormWasSubmitted,
}: ScienceCreateCategoryProps) => {
  return <div className="snipets-wrapper">create category science</div>;
};
